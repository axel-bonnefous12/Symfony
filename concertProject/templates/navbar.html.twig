<nav class="navbar navbar-expand-md navbar-dark bg-dark">
    <div class="navbar-collapse collapse w-100 order-1 order-md-0 dual-collapse2">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link active" aria-current="page" href="{{ path('bands_index') }}" style="color: darkgrey">Bands</a>
            </li>
            <li class="nav-item">
                <a class="nav-link active" aria-current="page" href="{{ path('concerts_list') }}" style="color: darkgrey">Concerts</a>
            </li>
            <li class="nav-item">
                <a class="nav-link active" aria-current="page" href="{{ path('artists_index') }}" style="color: darkgrey">Artists</a>
            </li>
        </ul>
    </div>
    <div class="mx-auto order-0">
        <a class="navbar-brand mx-auto" href="{{ path('concerts') }}" style="color: #FFFFFF">Home</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target=".dual-collapse2">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
    <div class="navbar-collapse collapse w-100 order-3 dual-collapse2">
        <ul class="navbar-nav ml-auto">
            <li class="nav-item">
                {% if app.user %}
                    <p class="badge bg-light text-wrap">Bonjour, {{ app.user.firstName() }} !</p>
                    <a href="{{ path('app_logout') }}" class="btn btn-light" role="button">Disconnect</a>
                {% else %}
                    <a href="{{ path('app_login') }}" class="btn btn-light" role="button">Connect</a>
                    <a href="{{ path('app_register') }}" class="btn btn-light" role="button">Register</a>
                {% endif %}
            </li>
        </ul>
    </div>
</nav>

<style>
        a:focus, a:hover
        {
            color: white!important;
        }
</style>

