{% extends 'base.html.twig' %}

{% block title %}Artists{% endblock %}

{% block body %}

    <br><br><br><br><br><br>
    <div class="container-lg">
            <div class="row">
                <div class="col">
                </div>
                <div class="col">
                    <table class="table table-dark table-striped">
                        <tbody>

                        <tr>
                            <th>Id</th>
                            <td>{{ artist.id }}</td>
                        </tr>
                        <tr>
                            <th>Name</th>
                            <td>{{ artist.name }}</td>
                        </tr>
                        <tr>
                            <th>FirstName</th>
                            <td>{{ artist.firstName }}</td>
                        </tr>
                        </tbody>
                    </table>
                </div>
                <div class="col">
                </div>
        </div>
    </div>




    <div class="container">
        <div class="row">
            <div class="col-12">
                <div class="d-flex align-items-center justify-content-center">
                    <a class='btn btn-secondary' href="{{ path('artists_index') }}">back to list</a>
                    <a class='btn btn-dark' href="{{ path('artists_edit', {'id': artist.id}) }}">edit</a>
                    {{ include('artists/_delete_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>



{% endblock %}
